spring:
  h2:
    console:
      enabled: true
    datasource:
      url: jdbc:h2:mem:testdb
  application:
    name: my-application
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  

   
